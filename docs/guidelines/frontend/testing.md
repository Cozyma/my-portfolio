# テスト（フロント）

目的: クリティカルな UI/ロジックの回 regress を防止。v0 は必要最小限から開始。

推奨方針
- ツール: Vitest + React Testing Library（導入は後日。現状は手動確認で十分）。
- 対象: API クライアントの失敗時メッセージ表示、フォームのバリデーション、重要な UI の可視性/状態。

メモ
- E2E（Playwright/Cypress）は安定後に検討。まずは単体/コンポーネント寄りで十分。

